<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context="com.maseno.franklinesable.lifereminder.SplashActivity"
    >
    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/splashTitle"
        android:text="@string/app_name"
        android:textAllCaps="true"
        android:textSize="45sp"
        android:textColor="#50BFE6"
        android:textStyle="bold"
        android:gravity="center"
        android:keepScreenOn="true"/>

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows.  //tools:ignore="UselessParent"-->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="@string/splash_version"
        android:id="@+id/version_indicator"
        android:layout_gravity="end|bottom"
        android:typeface="sans"
        android:textColor="@android:color/white"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_popup_reminder"
        android:tint="#50BFE6"
        android:id="@+id/splashIcon"
        android:layout_gravity="start|top"
        android:contentDescription="@string/app_name"/>

</FrameLayout>
